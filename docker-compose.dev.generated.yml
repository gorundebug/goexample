# Code generated by servicegen. DO NOT EDIT.
# This overlay is the only project-level Compose configuration that mounts
# service source code. Use it through `make docker-up-dev`.
services:
  analyticsservice:
    image: analyticsservice-development:latest
    build:
      context: .
      dockerfile: analyticsservice/Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICE_DIR: analyticsservice
        GOPROXY: ${GOPROXY:-direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        servicelib-source: "${GOSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/servicelib.git#v0.2.24}"
    working_dir: /workspace/analyticsservice
    command:
      - go
      - run
      - ./cmd/service/main.go
      - -config
      - ./config/config.yaml
      - -values
      - ./config/overrides.yaml
    environment:
      GOPROXY: ${GOPROXY:-direct}
      GOSUMDB: ${GOSUMDB:-sum.golang.org}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace
      - go-dev-mod-cache:/go/pkg/mod
      - go-dev-build-cache:/root/.cache/go-build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  automationservice:
    image: automationservice-development:latest
    build:
      context: .
      dockerfile: automationservice/Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICE_DIR: automationservice
        GOPROXY: ${GOPROXY:-direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        servicelib-source: "${GOSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/servicelib.git#v0.2.24}"
    working_dir: /workspace/automationservice
    command:
      - go
      - run
      - ./cmd/service/main.go
      - -config
      - ./config/config.yaml
      - -values
      - ./config/overrides.yaml
    environment:
      GOPROXY: ${GOPROXY:-direct}
      GOSUMDB: ${GOSUMDB:-sum.golang.org}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace
      - go-dev-mod-cache:/go/pkg/mod
      - go-dev-build-cache:/root/.cache/go-build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  inventoryservice:
    image: inventoryservice-development:latest
    build:
      context: .
      dockerfile: inventoryservice/Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICE_DIR: inventoryservice
        GOPROXY: ${GOPROXY:-direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        servicelib-source: "${GOSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/servicelib.git#v0.2.24}"
    working_dir: /workspace/inventoryservice
    command:
      - go
      - run
      - ./cmd/service/main.go
      - -config
      - ./config/config.yaml
      - -values
      - ./config/overrides.yaml
    environment:
      GOPROXY: ${GOPROXY:-direct}
      GOSUMDB: ${GOSUMDB:-sum.golang.org}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace
      - go-dev-mod-cache:/go/pkg/mod
      - go-dev-build-cache:/root/.cache/go-build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
  orderservice:
    image: orderservice-development:latest
    build:
      context: .
      dockerfile: orderservice/Dockerfile
      target: development
      extra_hosts:
        - "host.docker.internal:host-gateway"
      args:
        SERVICE_DIR: orderservice
        GOPROXY: ${GOPROXY:-direct}
        GOSUMDB: ${GOSUMDB:-sum.golang.org}
        SERVICEGEN_GITHUB_RAW_URL: ${SERVICEGEN_GITHUB_RAW_URL:-https://github.com}
      additional_contexts:
        servicelib-source: "${GOSERVICELIB_SOURCE_CONTEXT:-https://github.com/gorundebug/servicelib.git#v0.2.24}"
    working_dir: /workspace/orderservice
    command:
      - go
      - run
      - ./cmd/service/main.go
      - -config
      - ./config/config.yaml
      - -values
      - ./config/overrides.yaml
    environment:
      GOPROXY: ${GOPROXY:-direct}
      GOSUMDB: ${GOSUMDB:-sum.golang.org}
      GIT_CONFIG_COUNT: ${GIT_CONFIG_COUNT:-0}
      GIT_CONFIG_KEY_0: ${GIT_CONFIG_KEY_0:-}
      GIT_CONFIG_VALUE_0: ${GIT_CONFIG_VALUE_0:-}
      GIT_CONFIG_KEY_1: ${GIT_CONFIG_KEY_1:-}
      GIT_CONFIG_VALUE_1: ${GIT_CONFIG_VALUE_1:-}
    volumes:
      - .:/workspace
      - go-dev-mod-cache:/go/pkg/mod
      - go-dev-build-cache:/root/.cache/go-build
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE

volumes:
  go-dev-mod-cache:
  go-dev-build-cache:
  rust-dev-cargo-registry:
  rust-dev-cargo-git:
  rust-dev-target:
  cpp-dev-cmake-build: